node_modules/
dist/
.DS_Store
*.log
*.tsbuildinfo

# Secrets — DATABASE_URL, SESSION_SECRET, etc. live in a local .env now that
# the backend is Postgres-backed (see backend/.env.example for the template).
.env
.env.*
!.env.example
