{
  "name": "internez-extension",
  "version": "0.1.0",
  "private": true,
  "description": "Companion browser extension — autofills known application-form fields from an InternEZ profile.",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^2.1.9"
  }
}
